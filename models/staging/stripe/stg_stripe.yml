version: 2

models:
  - name: stg_payments
    description: One transacttion payment per row
    columns: 
      - name: payment_id
        description: Primary Id
        tests:
          - unique
          - not_null
